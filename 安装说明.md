# Murmur 安装说明（0.1 内测版）

Murmur 是一个常驻 macOS 菜单栏的小工具，做三件事：

- **框选翻译**：按 `⌘E`，在屏幕上拖一个框，框里的文字翻译出来
- **划词翻译**：在任何 App 里选中文字，按 `⌥S` 直接翻译
- **语音输入**：在输入框里按 `Home`，说话，文字自动填进去

---

## 开始之前

### 一、检查你的 Mac

**只支持 Apple 芯片的 Mac。** 点左上角  →「关于本机」，看到「芯片 Apple M1 / M2 / M3 / M4」才能用。如果那里写的是「处理器 Intel」，这一版装不了。

系统需要 **macOS 14 (Sonoma) 或更新**。

### 二、你需要自备一个 API Key

**Murmur 不含任何账号或额度，翻译和语音识别用的是你自己的 AI 服务账号。** 安装包里没有预置任何密钥。

如果你还没有，推荐硅基流动（注册即送额度，够试很久）：

1. 打开 https://cloud.siliconflow.cn/account/ak
2. 注册后新建一个 API 密钥，复制备用

其他支持的服务：OpenAI、DeepSeek、OpenRouter、月之暗面、智谱、阿里云百炼、Groq。

---

## 第一步：安装

1. 双击下载好的 `Murmur-0.1.dmg`
2. 把 **Murmur** 图标拖到旁边的 **Applications（应用程序）** 文件夹上
3. 拖完后，把这个窗口关掉，并在左侧边栏把 Murmur 磁盘推出

---

## 第二步：首次打开（系统会拦一次，这是正常的）

这一版没有经过 Apple 的付费开发者认证，所以第一次打开时 macOS 会拦住它。**这不代表软件有问题**，只是苹果对未付费认证的软件一律如此。

### 方法 A：点几下（推荐）

1. 在「应用程序」里双击 Murmur
2. 弹出提示说无法验证开发者 —— 点「**完成**」，**不要点「移到废纸篓」**
3. 打开「**系统设置**」→「**隐私与安全性**」
4. 往下滚到接近底部的「**安全性**」区域，会看到一行写着 Murmur 被阻止
5. 点旁边的「**仍要打开**」
6. 再次确认，输入开机密码或用触控 ID

以后再打开就不会拦了。

> 网上很多教程说「右键点图标 → 打开」可以绕过。**那个方法从 macOS 15 开始已经失效了**，别浪费时间。

### 方法 B：用终端一行命令（熟悉终端的话更快）

打开「终端」，粘贴这行后回车：

```
xattr -dr com.apple.quarantine /Applications/Murmur.app
```

然后正常双击打开即可。

---

## 第三步：授予三项权限

Murmur 打开后会引导你开启权限。三项分别用来做什么：

| 权限 | 用来做什么 | 不给会怎样 |
|---|---|---|
| **屏幕录制** | 框选翻译要截取你框的那块屏幕 | `⌘E` 用不了 |
| **辅助功能** | 读取你选中的文字、把语音结果填进输入框 | `⌥S` 和语音自动填入用不了 |
| **麦克风** | 语音输入要录音 | 语音输入用不了 |

每一项都要在「系统设置 →隐私与安全性」里找到对应项，把 Murmur 打开。

**Murmur 不会上传你的屏幕内容或录音到我这里**——它只把你主动框选的图片、选中的文字、或录下的语音，发给**你自己配置的那个 AI 服务**。

---

## 第四步：配置模型

首次打开会自动出现三步引导：**授权 → 接入模型 → 试一下**。

在第二步里选一个服务商（比如硅基流动），粘贴你的 API Key，选模型。推荐配置：

- **翻译**：`deepseek-ai/DeepSeek-V3.2`
- **语音识别**：`Qwen/Qwen3-ASR-1.7B`

第三步可以立刻试一次，确认能跑通。

装好后 Murmur 会出现在屏幕**底部程序坞**和**右上角菜单栏**，两个入口都能用。

---

## ⚠️ 重要：每次更新都要重新授权一遍

这一版用的是临时签名。**每次你装新版本，上面那三项权限都会自动失效，需要全部重新添加一遍。**

重加的正确做法是：在「系统设置 → 隐私与安全性」里，把对应项里的 Murmur **先用减号删掉，再用加号重新添加**——只是把开关关掉再打开是没用的。

这是内测阶段的已知代价，等正式版做了苹果认证后就不会再有这个问题。如果你更新后发现「突然不好使了」，先来这里检查。

---

## 遇到问题

- **双击没反应 / 提示已损坏**：先确认你的 Mac 是 Apple 芯片，再回第二步走一遍
- **翻译报错**：多半是 API Key 没填对或者额度用完了，去「设置 → 模型」里检查
- **语音没声音 / 识别是空的**：戴蓝牙耳机时链路要 1.5 秒才接通，等浮条显示「正在录音」再开口
- **划词翻译没反应**：确认辅助功能权限给了；另外对着 Murmur 自己的窗口按 `⌥S` 是不会翻译的，要在别的 App 里选文字

其他问题欢迎在本仓库提 [Issue](../../issues)，麻烦附上你的 macOS 版本和 Murmur 版本号。

---

**版本** 0.1 ｜ **大小** 2.4 MB ｜ **校验值 (SHA256)**
`ca726b6e3e2c367bae2247b8e8deb9c41c491cc086a0ba997877acb11352d57f`
